'use strict';

const fs = require('fs');
const bitmap = require('./lib/bitMap');
const transform = require('./lib/transform');

// TODO: Explain how this works (in your README)

transform.transformWithCallbacks();
